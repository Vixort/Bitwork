/**
 * =============================================================================
 * CommunityMain.jsx - Community Forum Page (หน้าชุมชน/ฟอรัม)
 * =============================================================================
 *
 * Component นี้เป็น Container หลักของหน้า Community Forum
 * รวมทุก Component และจัดการ State ของหน้านี้
 *
 * ทำหน้าที่:
 * 1. จัดการ State กลาง (activeCategory, searchQuery)
 * 2. เก็บข้อมูล Mock Data (categories, posts, trending, contributors)
 * 3. Filter และส่งข้อมูลไปยัง Child Components
 * 4. รวม Components ย่อยทั้งหมด
 *
 * โครงสร้างหน้า:
 * └── community-page
 *     ├── HeroSection - ส่วน Hero พร้อม Search
 *     └── community-main
 *         └── community-container (3 คอลัมน์)
 *             ├── CategorySidebar (ซ้าย) - หมวดหมู่
 *             ├── PostFeed (กลาง) - รายการกระทู้
 *             └── RightSidebar (ขวา) - Trending & Contributors
 *
 * URL: http://localhost:5173/community
 *
 */

import React, { useState, useEffect } from "react";

/**
 * AOS (Animate On Scroll) Library
 * - ใช้สำหรับ Animation เมื่อ scroll
 * - ทำให้ UI มีชีวิตชีวา
 */
import AOS from "aos";
import "aos/dist/aos.css";

// =============================================================================
// COMPONENT IMPORTS - นำเข้า Components ย่อย
// =============================================================================

/**
 * HeroSection - ส่วน Hero ของหน้า Community
 * - แสดงข้อความต้อนรับ
 * - มี Search Box สำหรับค้นหากระทู้
 *
 * Props ที่รับ:
 * - searchQuery: ค่าปัจจุบันของช่องค้นหา
 * - setSearchQuery: ฟังก์ชันอัพเดทค่าค้นหา
 */
import HeroSection from "./components/HeroSection";

/**
 * CategorySidebar - แถบหมวดหมู่ด้านซ้าย
 * - แสดงรายการหมวดหมู่ทั้งหมด
 * - กดเลือกหมวดหมู่เพื่อ Filter กระทู้
 *
 * Props ที่รับ:
 * - categories: รายการหมวดหมู่ทั้งหมด
 * - activeCategory: หมวดหมู่ที่เลือกอยู่
 * - setActiveCategory: ฟังก์ชันเปลี่ยนหมวดหมู่
 */
import CategorySidebar from "./components/CategorySidebar";

/**
 * PostFeed - ส่วนแสดงกระทู้หลัก (ตรงกลาง)
 * - แสดงรายการกระทู้ที่ Filter แล้ว
 * - ใช้ PostCard Component สำหรับแต่ละกระทู้
 *
 * Props ที่รับ:
 * - posts: รายการกระทู้ที่จะแสดง
 * - formatDate: ฟังก์ชัน format วันที่
 * - formatNumber: ฟังก์ชัน format ตัวเลข
 */
import PostFeed from "./components/PostFeed";

/**
 * RightSidebar - แถบด้านขวา
 * - แสดง Trending Topics
 * - แสดง Top Contributors
 *
 * Props ที่รับ:
 * - trendingTopics: หัวข้อยอดนิยม
 * - topContributors: ผู้ใช้ที่ Active มากที่สุด
 * - formatNumber: ฟังก์ชัน format ตัวเลข
 */
import RightSidebar from "./components/RightSidebar";

/**
 * CommunityMain.css - Styles สำหรับหน้า Community
 * - Layout 3 คอลัมน์
 * - Responsive Design
 */
import "./CommunityMain.css";

// =============================================================================
// COMMUNITYMAIN COMPONENT
// =============================================================================

/**
 * CommunityMain Component
 *
 * @description Container หลักของหน้า Community Forum
 * @returns {JSX.Element} - หน้า Community พร้อม Components ทั้งหมด
 *
 * State:
 * - activeCategory: หมวดหมู่ที่เลือก (default: "ทั้งหมด")
 * - searchQuery: คำค้นหา (default: "")
 *
 * การทำงาน:
 * 1. เก็บข้อมูล Mock Data
 * 2. Filter กระทู้ตาม Category และ Search
 * 3. ส่งข้อมูลไปยัง Child Components
 */
const CommunityMain = () => {
  // =============================================================================
  // STATE MANAGEMENT - จัดการ State
  // =============================================================================

  /**
   * activeCategory State
   * - เก็บหมวดหมู่ที่ผู้ใช้เลือกอยู่
   * - ใช้สำหรับ Filter กระทู้
   * - ค่าเริ่มต้น: "ทั้งหมด" (แสดงทุกกระทู้)
   */
  const [activeCategory, setActiveCategory] = useState("ทั้งหมด");

  /**
   * searchQuery State
   * - เก็บคำค้นหาจาก Search Box
   * - ใช้สำหรับ Filter กระทู้ตาม title, excerpt, tags
   * - ค่าเริ่มต้น: "" (ไม่มีคำค้นหา)
   */
  const [searchQuery, setSearchQuery] = useState("");

  // =============================================================================
  // SIDE EFFECTS - ผลข้างเคียง (useEffect)
  // =============================================================================

  /**
   * useEffect สำหรับ AOS Initialization
   *
   * การทำงาน:
   * - ทำงานครั้งเดียวเมื่อ Component mount
   * - เริ่มต้น AOS Library
   *
   * AOS Config:
   * - duration: 800 = Animation 800ms
   * - once: true = ทำงานครั้งเดียว
   * - easing: "ease-out-cubic" = รูปแบบ Animation
   */
  useEffect(() => {
    AOS.init({
      duration: 800,
      once: true,
      easing: "ease-out-cubic",
    });
  }, []);

  // =============================================================================
  // MOCK DATA - ข้อมูลตัวอย่าง (จะเปลี่ยนเป็น API ในอนาคต)
  // =============================================================================

  /**
   * categories - รายการหมวดหมู่ทั้งหมด
   *
   * แต่ละหมวดหมู่ประกอบด้วย:
   * - name: ชื่อหมวดหมู่
   * - icon: Emoji แสดงหมวดหมู่
   * - count: จำนวนกระทู้ในหมวดหมู่นี้
   */
  const categories = [
    { name: "ทั้งหมด", icon: "📋", count: 1250 },
    { name: "ถาม-ตอบ", icon: "❓", count: 458 },
    { name: "แชร์ประสบการณ์", icon: "💡", count: 324 },
    { name: "รีวิวบริษัท", icon: "🏢", count: 189 },
    { name: "เงินเดือน", icon: "💰", count: 276 },
    { name: "เทคโนโลยี", icon: "💻", count: 412 },
    { name: "Career Path", icon: "🚀", count: 167 },
    { name: "Remote Work", icon: "🏠", count: 98 },
  ];

  /**
   * posts - รายการกระทู้ทั้งหมด
   *
   * แต่ละกระทู้ประกอบด้วย:
   * - id: รหัสกระทู้ (unique)
   * - title: หัวข้อกระทู้
   * - excerpt: ตัวอย่างเนื้อหา
   * - author: ชื่อผู้เขียน
   * - authorAvatar: รูป Avatar ผู้เขียน
   * - date: วันที่โพสต์
   * - category: หมวดหมู่
   * - tags: Tags ของกระทู้
   * - views: จำนวนการดู
   * - replies: จำนวนความคิดเห็น
   * - likes: จำนวน Likes
   * - isPinned: ปักหมุดหรือไม่
   * - isHot: เป็นกระทู้ Hot หรือไม่
   */
  const posts = [
    {
      id: 1,
      title:
        "รีวิวการทำงานที่ Google Thailand หลังจากทำงานมา 2 ปี - ข้อดีข้อเสียแบบเรียลๆ",
      excerpt:
        "สวัสดีครับ วันนี้จะมาแชร์ประสบการณ์การทำงานที่ Google Thailand หลังจากที่ทำมาครบ 2 ปีพอดี ตั้งแต่วันแรกที่เข้าทำงาน...",
      author: "TechGuru2024",
      authorAvatar: "https://i.pravatar.cc/100?img=1",
      date: "2024-01-15",
      category: "รีวิวบริษัท",
      tags: ["Google", "BigTech", "Review"],
      views: 15420,
      replies: 89,
      likes: 342,
      isPinned: true,
      isHot: true,
    },
    {
      id: 2,
      title: "Junior Developer ควรเรียนรู้อะไรบ้างในปี 2024?",
      excerpt:
        "กำลังจะจบใหม่ปีนี้ครับ อยากถามพี่ๆ ว่าควรเริ่มต้นจากอะไรดี ระหว่าง React, Vue, หรือ Angular ปัจจุบันตลาดงานต้องการอะไรมากกว่ากัน...",
      author: "DevNewbie",
      authorAvatar: "https://i.pravatar.cc/100?img=2",
      date: "2024-01-14",
      category: "ถาม-ตอบ",
      tags: ["Junior", "Learning", "Career"],
      views: 8932,
      replies: 156,
      likes: 287,
      isPinned: false,
      isHot: true,
    },
    {
      id: 3,
      title:
        "แชร์ประสบการณ์ย้ายจาก Backend ไป DevOps - เงินเดือนเพิ่มขึ้นเท่าไหร่?",
      excerpt:
        "หลังจากทำ Backend มา 3 ปี ตัดสินใจเปลี่ยนสาย ไป DevOps ครับ วันนี้มาแชร์ว่าต้องเตรียมตัวยังไง และเงินเดือนต่างกันแค่ไหน...",
      author: "CloudMaster",
      authorAvatar: "https://i.pravatar.cc/100?img=3",
      date: "2024-01-13",
      category: "Career Path",
      tags: ["DevOps", "Backend", "Salary"],
      views: 12890,
      replies: 94,
      likes: 421,
      isPinned: false,
      isHot: false,
    },
    {
      id: 4,
      title: "เปรียบเทียบ Salary Range ของ Developer ในไทย 2024",
      excerpt:
        "รวบรวมข้อมูลเงินเดือนจากหลายๆ แหล่ง มาทำเป็น Salary Range ให้พี่น้องได้เปรียบเทียบกันครับ ตั้งแต่ Junior ยัน Senior...",
      author: "SalaryHunter",
      authorAvatar: "https://i.pravatar.cc/100?img=4",
      date: "2024-01-12",
      category: "เงินเดือน",
      tags: ["Salary", "Thailand", "Developer"],
      views: 25670,
      replies: 234,
      likes: 892,
      isPinned: true,
      isHot: true,
    },
    {
      id: 5,
      title: "Remote Work จากต่างจังหวัด ทำยังไงให้ Productive และไม่ Burnout",
      excerpt:
        "ทำงาน Remote จากเชียงใหม่มาได้ปีกว่าแล้วครับ มีทั้งข้อดีและความท้าทาย วันนี้มาแชร์เทคนิคที่ช่วยให้ทำงานได้มีประสิทธิภาพ...",
      author: "RemoteWarrior",
      authorAvatar: "https://i.pravatar.cc/100?img=5",
      date: "2024-01-11",
      category: "Remote Work",
      tags: ["Remote", "WFH", "Productivity"],
      views: 6543,
      replies: 67,
      likes: 198,
      isPinned: false,
      isHot: false,
    },
    {
      id: 6,
      title: "React 19 มีอะไรใหม่บ้าง? สรุปแบบเข้าใจง่าย",
      excerpt:
        "React 19 เพิ่งปล่อย RC มาไม่นาน มี Feature ใหม่เพียบ ทั้ง Actions, use API, Server Components และอีกเยอะ มาดูกันว่ามีอะไรบ้าง...",
      author: "ReactFan",
      authorAvatar: "https://i.pravatar.cc/100?img=6",
      date: "2024-01-10",
      category: "เทคโนโลยี",
      tags: ["React", "Frontend", "JavaScript"],
      views: 9876,
      replies: 45,
      likes: 267,
      isPinned: false,
      isHot: false,
    },
  ];

  /**
   * trendingTopics - หัวข้อที่กำลังเป็นที่นิยม
   *
   * แต่ละ Topic ประกอบด้วย:
   * - tag: Hashtag ของหัวข้อ
   * - posts: จำนวนกระทู้ที่เกี่ยวข้อง
   */
  const trendingTopics = [
    { tag: "#React19", posts: 234 },
    { tag: "#AI", posts: 189 },
    { tag: "#SalaryTalk", posts: 156 },
    { tag: "#RemoteWork", posts: 134 },
    { tag: "#Interview", posts: 98 },
  ];

  /**
   * topContributors - ผู้ใช้ที่ Active มากที่สุด
   *
   * แต่ละ Contributor ประกอบด้วย:
   * - name: ชื่อผู้ใช้
   * - avatar: รูป Avatar
   * - points: คะแนนสะสม
   */
  const topContributors = [
    {
      name: "TechGuru2024",
      avatar: "https://i.pravatar.cc/100?img=1",
      points: 12500,
    },
    {
      name: "CloudMaster",
      avatar: "https://i.pravatar.cc/100?img=3",
      points: 9870,
    },
    {
      name: "SalaryHunter",
      avatar: "https://i.pravatar.cc/100?img=4",
      points: 8540,
    },
    {
      name: "ReactFan",
      avatar: "https://i.pravatar.cc/100?img=6",
      points: 7230,
    },
    {
      name: "DevNewbie",
      avatar: "https://i.pravatar.cc/100?img=2",
      points: 5120,
    },
  ];

  // =============================================================================
  // HELPER FUNCTIONS - ฟังก์ชันช่วยเหลือ
  // =============================================================================

  /**
   * formatDate - แปลงวันที่เป็นรูปแบบภาษาไทย
   *
   * @param {string} dateString - วันที่ในรูปแบบ "YYYY-MM-DD"
   * @returns {string} - วันที่ในรูปแบบภาษาไทย
   *
   * การทำงาน:
   * 1. คำนวณความต่างของวันระหว่างวันที่กำหนดกับวันนี้
   * 2. แปลงเป็นข้อความที่เหมาะสม:
   *    - 1 วัน = "เมื่อวาน"
   *    - < 7 วัน = "X วันที่แล้ว"
   *    - < 30 วัน = "X สัปดาห์ที่แล้ว"
   *    - >= 30 วัน = "X เดือนที่แล้ว"
   */
  const formatDate = (dateString) => {
    const date = new Date(dateString);
    const now = new Date();
    const diffTime = Math.abs(now - date);
    const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
    if (diffDays === 1) return "เมื่อวาน";
    if (diffDays < 7) return diffDays + " วันที่แล้ว";
    if (diffDays < 30) return Math.floor(diffDays / 7) + " สัปดาห์ที่แล้ว";
    return Math.floor(diffDays / 30) + " เดือนที่แล้ว";
  };

  /**
   * formatNumber - แปลงตัวเลขเป็นรูปแบบย่อ
   *
   * @param {number} num - ตัวเลขที่ต้องการ format
   * @returns {string} - ตัวเลขในรูปแบบย่อ
   *
   * การทำงาน:
   * - ถ้า >= 1000 จะแสดงเป็น Xk (เช่น 1500 = 1.5k)
   * - ถ้า < 1000 แสดงตัวเลขปกติ
   */
  const formatNumber = (num) => {
    if (num >= 1000) {
      return (num / 1000).toFixed(1) + "k";
    }
    return num.toString();
  };

  // =============================================================================
  // FILTERED DATA - ข้อมูลที่ Filter แล้ว
  // =============================================================================

  /**
   * filteredPosts - กระทู้ที่ Filter ตาม Category และ Search Query
   *
   * การทำงาน:
   * 1. Filter ตาม activeCategory:
   *    - ถ้า "ทั้งหมด" = แสดงทุกกระทู้
   *    - ถ้าหมวดหมู่เฉพาะ = แสดงเฉพาะที่ตรงกัน
   * 2. Filter ตาม searchQuery:
   *    - ค้นหาใน title, excerpt, และ tags
   *    - Case insensitive
   */
  const filteredPosts = posts.filter((post) => {
    // ตรวจสอบหมวดหมู่
    const matchCategory =
      activeCategory === "ทั้งหมด" || post.category === activeCategory;

    // ตรวจสอบคำค้นหา (title, excerpt, หรือ tags)
    const matchSearch =
      post.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
      post.excerpt.toLowerCase().includes(searchQuery.toLowerCase()) ||
      post.tags.some((tag) =>
        tag.toLowerCase().includes(searchQuery.toLowerCase())
      );

    // ต้องผ่านทั้ง 2 เงื่อนไข
    return matchCategory && matchSearch;
  });

  // =============================================================================
  // RENDER - แสดงผล Component
  // =============================================================================

  return (
    <div className="community-page">
      {/* =================================================================
          HERO SECTION
          - ส่วน Hero ด้านบน
          - มี Search Box สำหรับค้นหากระทู้
      ================================================================= */}
      <HeroSection searchQuery={searchQuery} setSearchQuery={setSearchQuery} />

      {/* =================================================================
          MAIN CONTENT
          - เนื้อหาหลักแบบ 3 คอลัมน์
      ================================================================= */}
      <main className="community-main">
        <div className="community-container">
          {/* ----- LEFT SIDEBAR: Categories ----- */}
          {/* แสดงหมวดหมู่ทั้งหมด */}
          <CategorySidebar
            categories={categories}
            activeCategory={activeCategory}
            setActiveCategory={setActiveCategory}
          />

          {/* ----- CENTER: Post Feed ----- */}
          {/* แสดงรายการกระทู้ที่ Filter แล้ว */}
          <PostFeed
            posts={filteredPosts}
            formatDate={formatDate}
            formatNumber={formatNumber}
          />

          {/* ----- RIGHT SIDEBAR: Trending & Contributors ----- */}
          {/* แสดง Trending Topics และ Top Contributors */}
          <RightSidebar
            trendingTopics={trendingTopics}
            topContributors={topContributors}
            formatNumber={formatNumber}
          />
        </div>
      </main>
    </div>
  );
};

// Export CommunityMain Component เพื่อใช้ใน App.jsx
export default CommunityMain;
